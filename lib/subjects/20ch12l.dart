import 'package:flutter/material.dart';

class ChemistryLab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.greenAccent,
      child: Text('Chemistry Lab'),
    );
  }
}
